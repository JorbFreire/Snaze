#ifndef HANDLE_INPUT_H
#define HANDLE_INPUT_H

#include <iostream>
#include <string>
#include <vector>
#include <fstream>

void filter(vector < vector <string> > * maze, string &input);
std::vector < std::vector <std::string> > inputHandle (string directory);

#endif