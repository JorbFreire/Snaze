#include <iostream>
#include <vector>

int main(){

    // recebe inputs no terminal (que sera um diretorio)
        // guarda em uma variavel Y
    // formata/processa inputs
        // recebe como Y parametro
        // retorna matriz em uma variavel 
    // entra no game loop
    // faz o resto
    return 0;
}